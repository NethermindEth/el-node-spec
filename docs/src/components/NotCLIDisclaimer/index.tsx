import React from 'react';

export default function NotCLIDisclaimer() {
    return (
        <p>Commands are for demonstration purposes only as a Middleware setup wizard tool doesn't exists yet.</p>
    )
}
