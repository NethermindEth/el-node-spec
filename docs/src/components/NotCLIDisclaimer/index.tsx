import React from 'react';

export default function NotCLIDisclaimer() {
    return (
        <blockquote><em><strong>Note:</strong> Commands are for demonstration purposes only as the cli tool doesn't exists yet.</em></blockquote>
    );
}
